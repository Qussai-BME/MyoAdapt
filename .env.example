# Copy to .env and replace every placeholder before any network deployment.
# Never commit .env or a real API key.
MYOADAPT_API_KEY=replace-with-a-long-random-secret

# Optional. Comma-separated, explicit browser origins only; do not use *.
# MYOADAPT_CORS_ORIGINS=https://research.example.org
